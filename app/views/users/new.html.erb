estás en el new de users
<% if flash[:error] %>
	<%= flash[:error] %>
<%end%>
<h1>Register:</h1>
<% if @user.errors.any? %>
    <ul>
        <% @user.errors.full_messages.each do |error_message| %>
        <li> <%= error_message %></li>
        <% end %>
    </ul>
<% end %>
<%= form_for [@user] do |f| %>

	<br><%= f.label :user_name %>
	<br><%= f.text_field :user_name %>

	<br><%= f.label :email %>
	<br><%= f.text_field :email %>

	<br><%= f.submit "Save" %>
<% end %>